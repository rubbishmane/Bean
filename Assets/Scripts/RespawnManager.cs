using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RespawnManager : MonoBehaviour
{
    void Respawn(string currentMap)
    {
        ResetHealth();
    }
    void ResetHealth()
}
